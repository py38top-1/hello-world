# Harness Engineering 概念与落地

## 什么是 Harness Engineering

Harness Engineering（支架工程）是：围绕 AI 编码智能体搭建一套工程“控制系统”，让产出更稳定、可验证、可交付。

它关注的重点不是“写出更花哨的提示词”，而是“设计更可靠的工程环境”。

核心机制：
1. 人类定义目标、边界和质量标准。
2. 智能体执行具体实现。
3. 文档、测试、CI、规则对结果进行约束与校验。
4. 通过反馈闭环持续优化质量。

## 在真实开发中如何使用

1. 为智能体建立项目契约
在 `AGENTS.md` 中写清编码规范、提交规范、测试要求、禁止操作和目录约定。

2. 让上下文机器可读
把架构说明、接口契约、业务规则放入 `docs/`，并提供可执行示例。

3. 把标准变成自动化门禁
启用 lint、类型检查、单测、安全扫描、CI 必过，作为合并前硬约束。

4. 使用小任务拆解
给智能体明确、可验收的小范围任务，避免一次性大而模糊的需求。

5. 执行“实现-验证-评审”闭环
要求智能体先跑检查、总结改动与风险，再由人审核关键变更。

6. 失败即沉淀
一旦出错，把问题固化进规则、测试或模板，防止同类错误再次发生。

## 实际收益

通过 Harness Engineering，团队可以显著降低 AI 输出的随机性，减少回归问题，提高迭代速度和交付可预测性。

## 参考链接

1. https://openai.com/zh-Hans-CN/index/harness-engineering/
2. https://www.cnblogs.com/informatics/p/19812585

## 项目中落地 Harness Engineering（步骤版）

1. 明确目标与边界  
先定义业务目标、交付范围、完成标准（DoD），并写清不在本次范围内的事项，避免智能体误扩展需求。

2. 建立智能体工作契约  
在 `AGENTS.md` 中约定编码规范、目录约束、提交信息格式、测试要求、禁止命令与评审规则。

3. 沉淀可读上下文  
在 `docs/` 补齐架构说明、模块职责、接口契约、业务规则和示例输入输出，让智能体可检索、可引用。

4. 任务拆解为可验收单元  
将需求拆成小任务（每个任务有输入、输出、验收条件），减少一次性大改带来的不确定性。

5. 让质量门禁自动执行  
配置并强制执行 lint、格式化、类型检查、单测、集成测试和安全扫描，把“建议”变成“硬约束”。

6. 规定标准执行流程  
建议采用固定流程：`需求 -> 计划 -> 实现 -> 自测 -> CI -> 评审 -> 合并`，每一步都留可追溯记录。

7. 强化改动说明与风险披露  
要求每次变更附带“改了什么、为什么改、影响范围、回滚方式”，提高团队协作效率。

8. 用失败案例反哺规则  
出现问题后，不只修当前 bug，还要把教训沉淀到规则、测试、模板中，防止同类问题重复发生。

9. 周期性优化 Harness  
按迭代复盘：统计返工率、回归率、交付周期等指标，持续收紧高风险环节、简化低价值步骤。

10. 从小范围试点到全仓推广  
先在单一模块试运行，验证效果后逐步扩展到全项目，降低迁移成本与流程阻力。

## Laravel 项目实战：如何一步步使用 Harness Engineering

1. 定义 Laravel 项目目标  
例如：两周内上线“用户资料管理”模块，包含资料查看、编辑、头像上传、审计日志。

2. 约定工程边界与规范  
在 `AGENTS.md` 明确：
- Laravel 版本、PHP 版本、数据库版本。
- 必须遵循 PSR-12、业务逻辑放在 Service 层、Controller 保持薄层。
- 数据库变更必须通过 migration，不允许手改线上库。
- 涉及权限的接口必须使用 Policy 或 Gate。

3. 建立 Laravel 上下文文档  
在 `docs/` 增加：
- `docs/architecture.md`：模块分层与依赖规则。
- `docs/api-contract.md`：路由、请求参数、响应格式、错误码。
- `docs/domain-rules.md`：业务校验规则（如昵称长度、头像大小限制）。
- `docs/examples/`：请求与响应样例。

4. 固化代码脚手架与约束  
通过 Laravel 约定目录与命名：
- `app/Http/Controllers` 仅做请求转发与参数校验。
- `app/Services` 放业务编排。
- `app/Repositories`（如团队使用）处理数据访问抽象。
- `app/Policies` 管理权限。
- `app/DTO` 或 Form Request 统一输入结构。

5. 设计可执行任务模板  
每个任务都写清：
- 输入：需求与接口契约。
- 输出：代码文件列表、测试用例列表、迁移脚本。
- 验收：`php artisan test` 通过，关键接口有 Feature Test，核心逻辑有 Unit Test。

6. 配置本地与 CI 质量门禁  
推荐最小门禁：
- `composer test` 或 `php artisan test`
- `./vendor/bin/pint --test`
- `./vendor/bin/phpstan analyse`（如已接入）
- 安全检查（如 `composer audit`）
在 CI 中设置为必过后才能合并。

7. 用“小步快跑”驱动智能体开发  
示例任务流（用户资料编辑）：
- 第一步：生成 migration 与 model 字段调整。
- 第二步：实现 Form Request 校验规则。
- 第三步：实现 Service 更新逻辑与事务控制。
- 第四步：实现 Controller 与 API Resource 返回结构。
- 第五步：补 Feature Test 与 Unit Test。
每一步都先跑测试再进入下一步。

8. 强制改动说明标准化  
每次提交都要求包含：
- 变更摘要（改了什么）
- 变更原因（为什么改）
- 影响范围（接口、表结构、权限）
- 回滚方式（回退 migration 或关闭开关）

9. 处理失败并沉淀为规则  
例如出现“未授权用户能修改资料”的问题后：
- 补一个失败复现测试。
- 在 `AGENTS.md` 加入“所有写接口必须显式权限校验”。
- 在代码模板中默认加入 Policy 检查。

10. 周期性复盘与扩展  
每个迭代统计：
- 缺陷率、回归率、平均修复时长、PR 周期。
把高频问题反向更新到文档、测试、CI 规则，再复制到下一个模块（订单、支付、通知等）。

## FastAPI 项目实战：如何一步步使用 Harness Engineering

1. 明确 FastAPI 项目目标  
例如：在一个迭代内交付“任务管理 API”，包含任务创建、查询、更新状态、删除与权限控制。

2. 约定工程标准与边界  
在 `AGENTS.md` 明确：
- Python 版本、FastAPI 版本、数据库与迁移工具（如 Alembic）。
- 代码风格（Ruff/Black）、类型检查（mypy）和目录结构约定。
- 禁止直接在路由函数中堆积业务逻辑，服务层必须独立。
- 所有写操作接口必须做鉴权与输入校验。

3. 沉淀可检索上下文  
在 `docs/` 增加：
- `docs/architecture.md`：分层原则（router/service/repository/schema）。
- `docs/api-contract.md`：接口定义、状态码、错误响应模型。
- `docs/domain-rules.md`：业务约束（状态流转规则、字段边界）。
- `docs/examples/`：请求与响应样例（含异常场景）。

4. 固化 FastAPI 目录与职责  
建议目录示例：
- `app/api`：路由与依赖注入。
- `app/schemas`：Pydantic 请求/响应模型。
- `app/services`：业务编排与事务边界。
- `app/repositories`：数据库访问。
- `app/models`：ORM 模型。
- `app/core`：配置、日志、安全与公共组件。

5. 把任务拆成可验收小单元  
每个任务需包含：
- 输入：接口契约与业务规则。
- 输出：新增/修改文件清单、测试用例清单、迁移文件。
- 验收：`pytest` 通过，关键路径覆盖率达标，类型检查与 lint 通过。

6. 配置本地和 CI 门禁  
推荐最小门禁：
- `ruff check .`
- `ruff format --check .` 或 `black --check .`
- `mypy app`
- `pytest -q`
- 安全检查（如 `pip-audit`）
在 CI 设为必过条件，禁止跳过。

7. 采用“实现-验证-提交”小循环  
示例任务流（任务状态更新接口）：
- 第一步：定义 Pydantic schema 与状态枚举。
- 第二步：实现 service 状态流转逻辑（含非法流转保护）。
- 第三步：实现 router 与依赖注入。
- 第四步：补单元测试与 API 集成测试。
- 第五步：运行 lint/type/test 全套检查后提交。

8. 统一变更说明模板  
每次 PR 要包含：
- 改动内容与动机。
- 数据模型或接口变更影响。
- 风险点与回滚方案。
- 测试结果摘要（命令与通过情况）。

9. 失败案例回灌 Harness  
例如出现“未校验字段导致 500”：
- 先补失败复现测试。
- 在 schema 层补严格校验。
- 在 `AGENTS.md` 增加“所有外部输入必须有 Pydantic 校验”规则。
- 在模板任务中加入“异常路径测试”检查项。

10. 持续复盘并规模化推广  
每轮统计：接口缺陷率、回归率、平均交付周期、测试稳定性。  
把高频问题转化为自动化规则，再逐步推广到更多 FastAPI 模块或微服务。
