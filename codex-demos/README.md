# Codex Demos

This directory contains two Harness Engineering demo setups:

1. `laravel-demo`
2. `fastapi-demo`

Each demo includes:
- `AGENTS.md` for agent contract
- `docs/` for architecture and domain context
- `plans/` for task decomposition template
- `.github/workflows/quality.yml` for quality gates
